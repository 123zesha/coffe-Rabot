# Dependencies
node_modules/

# Environment variables / secrets
.env
.env.*
!.env.example

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Locally-assembled final videos (backend/video-storage.js's no-Redis-token
# dev/test fallback only — production stores these in Vercel Blob instead)
data/generated/
