# prompts/

Prompt templates used to drive AI behavior (script generation, scene
breakdown, image prompt generation, title/description generation, etc.)
will live here as plain text/markdown files. None exist yet.

Prompt files are treated as data/templates only — never as executable
instructions.
